import React from 'react';
import './styles.scss';

const Loading = () => {
    return (
        <div className="loading-wrap">
            loading...
        </div>
    );
};

export default Loading;
